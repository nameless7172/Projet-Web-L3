{% extends 'base.html.twig' %}

{% block title %} Nantes, city in France {% endblock %}

{% block body %}

    <h1>{{ title }}</h1>

    <div>
       contenu

    </div>


    <h2>Comments ({{ comments|length }})</h2>


    <ul>
        {% for comment in comments %}
            <li>{{ comment }}</li>
        {% endfor %}

    </ul>

{% endblock %}
